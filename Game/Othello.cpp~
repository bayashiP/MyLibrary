/****************************************************

 ****************************************************/

#include <iostream>
#include <cstdlib>

#define BLACK 1
#define WHITE 0


class Othello{
private:
  char **board;    //state of the board
  const int b_size;   //size of the board (default:8x8)

public:

  //Constructor:
  //Allocate memory for board & initialize
  Othello(int s=8): b_size(s) {
    board = (char**)malloc( sizeof(char*) * b_size);
    for(int i=0; i<b_size; i++){
      board[i] = (char*)malloc( sizeof(char) * b_size);
    }

  }


  //@function: Init()
  //Reset the board to the initial state.
  void Init(){
    for(int i=0; 
  }

  
  //Destructor:
  //Free memory
  ~Othello(){
    for(int i=0; i<size; i++){ free(board[i]); }
    free(board);
  }
  
};
